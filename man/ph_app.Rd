% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{ph_app}
\alias{ph_app}
\title{Launch the Shiny app.}
\usage{
ph_app(
  config = NULL,
  port = 7655L,
  launch_browser = interactive(),
  browser = NULL
)
}
\arguments{
\item{config}{A work directory, a config path, or a config list.}

\item{port}{Port to serve on.}

\item{launch_browser}{Open a browser window.}

\item{browser}{Which browser to open: \code{"Chrome"}, \code{"Firefox"}, a path to an
application, or \code{NULL} to let phostor choose one that can record.}
}
\value{
Invisibly, whatever \code{\link[shiny:runApp]{shiny::runApp()}} returned.
}
\description{
The config is resolved to absolute paths before launching, because
\code{\link[shiny:runApp]{shiny::runApp()}} changes the working directory to the app folder.
}
\details{
Blocks until the app stops.

Recording happens in the browser, so which browser opens matters. By default
phostor opens one that can record (Chrome, then Firefox) in preference to
the system default, which may be neither.
}
\examples{
\dontrun{
ph_app("~/phostor/family")
ph_app("~/phostor/family", browser = "Firefox")
}
}
