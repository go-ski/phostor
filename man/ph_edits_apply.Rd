% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edits.R
\name{ph_edits_apply}
\alias{ph_edits_apply}
\title{Apply a transcript's corrections to the transcriber's own phrases.}
\usage{
ph_edits_apply(timed, edits)
}
\arguments{
\item{timed}{Phrases as the transcriber wrote them: \code{start}, \code{end}, \code{text}.}

\item{edits}{An overlay from \code{\link[=ph_edits_read]{ph_edits_read()}}.}
}
\value{
A data.frame of \code{start}, \code{end} and \code{text}, ordered by \code{start}.
}
\description{
Pure: the substitution rule on its own, so it can be reasoned about and
tested without a project on disk. A phrase whose start matches an \code{orig} is
replaced by that group's rows; every other phrase is kept as it is. Rows
whose \code{orig} matches no phrase are dropped, which is what makes a correction
harmless after the recording has been transcribed again to different
boundaries.
}
\examples{
timed <- data.frame(start = 0, end = 4, text = "one two")
edits <- data.frame(orig = c(0, 0), start = c(0, 2), end = c(2, 4),
                    text = c("one", "two"))
ph_edits_apply(timed, edits)
}
