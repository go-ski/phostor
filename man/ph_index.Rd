% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index.R
\name{ph_index}
\alias{ph_index}
\title{Build or refresh the catalogue.}
\usage{
ph_index(config = NULL, quiet = FALSE)
}
\arguments{
\item{config}{A work directory, a config path, or a config list.}

\item{quiet}{Suppress progress messages.}
}
\value{
The catalogue, invisibly.
}
\description{
Scans \code{photo_root}, assigns an id to every photograph it has not seen
before, reads capture dates and dimensions in a single \code{exiftool} call, and
writes \code{index.tsv}.
}
\details{
Ids are stable: a photograph keeps the id it was first given, and a new one
takes the next number up. Nothing is keyed by position in the file, so
adding photographs does not renumber the rendered copies of existing ones.

Photographs no longer present in \code{photo_root} drop out of the catalogue.
Their sidecars are left in place.
}
\examples{
\dontrun{
idx <- ph_index("~/phostor/family")
}
}
