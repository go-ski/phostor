% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tags.R
\name{ph_tags_clean}
\alias{ph_tags_clean}
\title{Put a set of tags into their canonical shape.}
\usage{
ph_tags_clean(x)
}
\arguments{
\item{x}{A list with any of \code{people}, \code{place}, \code{event}, \code{when}; anything
else is ignored, and anything missing comes back empty.}
}
\value{
A list of \code{people} (a character vector), \code{place}, \code{event} and \code{when}
(single strings, \code{""} when unset).
}
\description{
Trims, drops empty names, and gives every field the type it should have, so
that two sets of tags can be compared. Exported because \code{inst/shiny/app.R}
calls it to tell an edit from the seeding of the fields, and \code{runApp()}
sources that file with only \code{library(phostor)} attached.
}
\examples{
ph_tags_clean(list(place = "  Elgol ", people = c("Vera", "", " Stefan")))
}
