% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transcribe.R
\name{ph_transcribe_build}
\alias{ph_transcribe_build}
\title{Build the transcription helper.}
\usage{
ph_transcribe_build(quiet = FALSE)
}
\arguments{
\item{quiet}{Suppress the compiler's own output on failure.}
}
\value{
The path to the binary, or \code{NA_character_} if it could not be built.
}
\description{
Compiles \code{inst/swift/transcribe.swift} into the user's cache directory. The
build is skipped when an up-to-date binary is already there, so this is
cheap to call on every start. An \code{Info.plist} is linked into the binary:
without one the process has no bundle identity and macOS cannot attribute a
speech-recognition request to it.
}
\examples{
\dontrun{
ph_transcribe_build()
}
}
