% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transcribe.R
\name{ph_transcribe_visit}
\alias{ph_transcribe_visit}
\title{Transcribe one visit's recording.}
\usage{
ph_transcribe_visit(
  cfg,
  rel_path,
  visit,
  audio = NULL,
  wait = FALSE,
  force = FALSE
)
}
\arguments{
\item{cfg}{A config list from \code{\link[=ph_config]{ph_config()}}.}

\item{rel_path}{Path of the photograph relative to \code{photo_root}.}

\item{visit}{Visit number.}

\item{audio}{The audio filename, as stored in the sidecar. Looked up from
the visit directory when \code{NULL}.}

\item{wait}{Wait for the transcript and return when it is written.}

\item{force}{Transcribe even if a transcript already exists.}
}
\value{
\code{"started"}, \code{"done"}, or the reason it was skipped, invisibly.
}
\description{
Started in the background by default: R is single-threaded, and waiting for
a transcript would freeze the app while the next photograph is on screen.
The helper writes \code{visit-NNNN.txt} beside the audio when it finishes.
}
\details{
Never raises. A visit that cannot be transcribed -- no audio, a WebM
recording, no helper on this machine -- returns the reason instead, so the
sitting carries on.
}
\examples{
\dontrun{
ph_transcribe_visit(ph_config(), "Trips/Skye/img_0421.jpg", 1)
}
}
